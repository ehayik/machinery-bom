buildscript {
    repositories {
        mavenCentral()
    }
}

plugins {
    id 'java-platform'
    id 'maven-publish'
}

repositories {
    mavenCentral()
}

apply plugin: 'java-platform'
apply plugin: 'maven-publish'
apply from: "$rootDir/gradle/ext/publish-common.gradle"

group = 'com.github.eljaiek.machinery'
version = '0.0.1-SNAPSHOT'

dependencies {
    constraints {
        api "com.github.eljaiek.nachinery:machinery-configmap-core:${project.version}"
        api "com.github.eljaiek.nachinery:machinery-configmap-jpa:${project.version}"
        api "com.github.eljaiek.nachinery:machinery-configmap-redis:${project.version}"
        api "com.github.eljaiek.nachinery:machinery-configmap-autoconfigure:${project.version}"
        api "com.github.eljaiek.nachinery:machinery-configmap-starter:${project.version}"
        api "com.github.eljaiek.nachinery:machinery-configmap-redis-starter:${project.version}"

        api "com.github.eljaiek.nachinery:machinery-web-starter:${project.version}"
        api "com.github.eljaiek.nachinery:machinery-web-i8ln:${project.version}"
    }
}

publishing {
    publications {
        mavenBom(MavenPublication) {
            from components.javaPlatform

            pom { x -> pom(x)}
        }
    }

    repositories {
        maven { x -> nexusRepository(x) }
    }
}